<h1>Listing posts</h1>

<%= link_to 'New Post', new_post_path, class: "button" %>

<% @posts.each do |post| %>
<hr>
<div class="row">
  <div class="columns large-4">
    <div class="row">
      <div class="columns large-8 text-right">
        <p><%= link_to post.user.username, post.user %><br>
          <%= time_ago_in_words(post.created_at) %></p>
      </div>
      <div class="columns large-4">
       <p><%= image_tag(post.user.profile.url(:thumb)) %></p>
      </div>
      
    </div>
    <div class="row">
      <div class="columns large-8 text-right">
        <p><%= post.user.location %></p>
      </div>
    </div>
  </div>
  <div class="columns large-8">
    <p><%= link_to image_tag(post.avatar.url :medium), post %></p>
    <p><%= post.description %></p>
  </div>
</div>

<% end %>



  <!--<table>
<thead>
    <tr>
      <th>Description</th>
      <th>User</th>
      <th>Fecha de creación</th>
      <th>Imagen</th>
      <th colspan="3">Acciones</th>
    </tr>
  </thead>



  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.description %></td>
        <td><%= post.user.username %></td>
        <td><%= post.created_at %></td>
        <td><%= image_tag post.avatar.url(:medium) %></td>
        <td><%= link_to 'Show', post %></td>
        <td><%= link_to 'Edit', edit_post_path(post) %></td>
        <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>-->




